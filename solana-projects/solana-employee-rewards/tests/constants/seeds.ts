// Global / system
export const GLOBAL_SEED = Buffer.from("global");
export const TREASURY_SEED = Buffer.from("treasury");

// Year / lifecycle
export const YEAR_SEED = Buffer.from("year");
export const SETTLEMENT_SEED = Buffer.from("settlement");

// Employee
export const EMPLOYEE_SEED = Buffer.from("employee");

// Task flow
export const TASK_SEED = Buffer.from("task");
export const ASSIGNMENT_SEED = Buffer.from("assignment");
export const COMPLETION_SEED = Buffer.from("completion");

// Reward tokens
export const REWARD_MINT_SEED = Buffer.from("reward-mint");
export const SPECIAL_MINT_SEED = Buffer.from("special-mint");

// Member status NFTs
export const MEMBER_STATUS_COLLECTION_MINT_SEED = Buffer.from(
  "member-status-collection-mint",
);
export const MEMBER_STATUS_NFT_SEED = Buffer.from("member-status-nft");
